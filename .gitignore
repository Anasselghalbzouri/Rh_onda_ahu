# Env racine pour docker-compose (contient APP_KEY — ne pas committer)
/.env

# Fichiers archivés (docs, rapports, captures, livrables, vba, .bat) — non nécessaires pour faire tourner le projet
/_archive/

# Claude / outils internes
/.claude
/CLAUDE.md
*.stackdump

backend/database/*.sqlite
